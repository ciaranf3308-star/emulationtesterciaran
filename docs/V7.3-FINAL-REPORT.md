# V7.3 Showroom Storefront – FINAL REPORT

Generated: 2026-08-09T23:59Z (IST)
Repo: https://github.com/ciaranf3308-star/emulationtesterciaran

## 23-field delivery

**CURRENT BASE SHA:** `99ffda49077d81d606f8f6a74efb51171a317ee1` (V7.2 `feat: V7.2 visual hierarchy` remote HEAD pre-V7.3, verified via `git ls-remote`)

**V7.3 SHA:** `28c895f1a2ee3c3edcf35503c272c546ed155015` – `feat: V7.3 showroom storefront – shelf RIGHT hero, outer wrapper, idle glass, live media bridge` – 15 files changed +1671/-374, push verified `99ffda4..28c895f main→main`, `git ls-remote origin main` matches local HEAD.

**STOREFRONT SHELF:** LEFT 26-31% viewport `width 29% min 26% max 31%` `src/components/SystemShelf.tsx` – very subtle transparent acrylic/glass rails, thin illuminated shelf edges, deep spatial layering, no opaque navy rectangle, no large bordered cards, no rounded SaaS tiles, no giant background panel. Shows 5 systems `prev-prev / prev / SELECTED / next / next-next` selected near vertical centre wrap cyclically `wrapIndex = ((i%len)+len)%len`. Neighbours recede opacity 0.68/0.38 scale 0.92/0.82 blur 0.3/1.1 shift left -8/-16, selected forward scale 1.08 sharp cool edge `drop-shadow 0 0 18px rgba(125,249,255,0.28)` faint reflection beneath. Utility rail small bottom-left beneath shelf for All/Favorites/Recent/Settings subtle icon/text controller-focus label – never span hardware.

**OLD BOTTOM CAROUSEL REMOVED:** `src/App.tsx` no longer contains `className="carousel"` nor `carouselRef` nor full-width bottom navy carousel – replaced by `SystemShelf` left hero right. Verified via `tests/v73.showroom.test.ts` structural test.

**HARDWARE HERO VISIBLE:** V7.2 stored hardware `opacity: entered ? 1 : showGuides?0.22:0` hidden – V7.3 corrects storefront hardware `opacity 1` always visible large unobstructed RIGHT 65-72% hero, outer `hardware-showroom-wrapper` around calibrated inner frame concept `viewport → hardware-showroom-wrapper (position/scale storefront 66/52 default scale 1.16 etc) → calibrated hardware-frame DO NOT BREAK → gameplay/physical/hardware`. `storefront showroom hero` comment.

**SHOWROOM OUTER TRANSFORM:** `src/stage/config/showroomDefaults.ts` categories: `handheld` x62 y52 scale1.16 64vw/78vh, `tv` x66 y52 1.18 72/74, `desktop` x68 y50 1.14 76/72, `hybrid` x64 y56 1.12 68/74 translateY12, `board` 62/52/1.1. Per-system overrides: gb 60/54/1.22 56vw portrait tall bold, gbc 60/54/1.22 58vw, gba 62/52/1.20 62vw/76vh larger centred-right handheld, psp 63/51/1.14 66vw, nds 60/52/1.18 54vw/82vh top 26.1%/17.4%/47.5%/23.0% bottom 27.8%/53.7%/43.9%/23.6%, n3ds 60/52/1.18 56vw/84vh top 31.6%/9.1%/36.7%/33.7% bottom 33.2%/55.7%/33.6%/29.9%, ps2 67/52/1.22 74vw/76vh CRT large right hero regression target, gc 66/51/1.20 70vw purple TV prominent, snes/n64/genesis/megadrive ~65/52/1.15, dreamcast 67/52/1.18, psx 66/52/1.17, xbox 67/53/1.16, xbox360 67/52/1.16, steam 69/49/1.12 78vw/70vh monitor-led wide, wii 66/52/1.14, wiiu 64/58/1.10 66vw translateY18 GamePad-led lower. Resolver `resolveShowroomPlacement(systemId, presentationType, explicit)` merges category → override → explicit defaults library 50/50/1. Wired into `src/stage/config.ts` via `SystemStageConfig.showroomPlacement`.

**INNER CALIBRATION PRESERVED:** Inner `hardware-frame` still computes FrameBounds from `showroomRef`/`stageRef` bounding rect via `ResizeObserver`, natural size via `new Image()`, contain math same as V7.1 `aImg=iw/ih aCont=cw/ch fw=aImg>aCont? cw : ch*aImg` no drift. Deterministic PS2 1920×1080 PNG 1254×1254 → frame left 420 top0 w1080 h1080 region vw left 613.3125 top116.625 w691.1875 h491.39 inFrame 63.9988% 45.4991% matches 17.9%/10.8%/64%/45.5% preserved, 1280×720 resize fw720 fh720 left280 top0 no drift. Wrapper left/top `%` from placementX/Y storefront 66/52 default vs library 50/50, `transform translate(-50%,-50%) scale(placementScale)` storefront vs `scale(libScale)` library duration `durFilter 420ms durTrans 560ms durHw 480ms durFade 380ms cubic-bezier(0.16,1,0.3,1)` meets 350-550ms 300-500ms spec. `.layer-gameplay` filter none razor sharp, physical filter none drop-shadow only.

**STORE IDLE SCREEN GLASS:** Storefront when no selected game media: `storefront-idle-glass` dark `linear-gradient 14,22,28 0.64 → 10,16,22 0.72 → 8,12,18 0.78 backdrop-filter blur(18px) brightness(0.92) saturate(1.06) border rgba(125,249,255,0.10) inset glow`, light similar cool-white pale-grey clear acrylic subtle blue/silver NOT beige/cream hospitality, smoked-glass layer + cyan reflection + faint highlight circle reduces background noise keeps transparent concept obvious not fake gameplay do not paint PNG or modify source asset.

**TOP DEV CHROME REMOVED:** Normal storefront reduced to small tasteful Crystal wordmark inside shelf + ghost toggle `◐` 28px circle top-right, dev chrome only when `?dev` or `localStorage crystal-dev=1` shows theme Toggle Guides Exit dev. Removed `ROG Ally X • real machine, system count, raw system ID beside platform name (e.g. ps2/gc), ROM extension list, emulator command label, No fake counts, full-width controller debug hint, Guides button, debug metadata` from normal storefront only in devMode.

**UTILITY DESTINATIONS:** Moved into restrained utility area associated with left shelf small bottom-left rail beneath shelf `.shelf-utility-rail` – never span across or obscure hardware. Never top-bar four pill buttons spanning.

**PLATFORM LOGO RESOLVER:** `src/components/SystemLogo.tsx` API `systemId, logoUrl, fallbackName, isSelected, theme` swappable provider for later Canvas logo set, uses `resolver.getThemeAssetsForSystem` temporary, fallback full name map `gb→Game Boy gbc→Game Boy Color gba→Game Boy Advance nds→Nintendo DS n3ds→Nintendo 3DS snes→Super Nintendo n64→N64 gc→GameCube wii→Wii wiiu→Wii U genesis→Genesis megadrive→Mega Drive dreamcast→Dreamcast psx→PlayStation ps2→PlayStation 2 psp→PlayStation Portable xbox→Xbox xbox360→Xbox 360 steam→Steam`, fallback title-case, never raw ID as hero branding.

**LIVE SELECTED GAME STATE:** App-level state `selectedGame: GameEntry|null`, `selectedGameplaySources`, `selectedPhysicalUrl`, `mediaResolving`. `SystemLibraryView` calls `onSelectGame` on idx change (highlighted game), `handleSelectGame` async Tauri-only `verifyMedia` 6 types `video/screenshot/titlescreen/miximage/cover/physicalmedia` → `toAssetUrl` → `pickGameplayFromResolved` truthful priority 1 video 2 screenshot/titlescreen/miximage/cover 3 premium idle. `CollectionView` accepts `onSelectGame`. Back clears bridge. Pre-wires to `SystemStage` `gameplaySources` single-screen `regionId main` / dual-screen truthful primary only.

**REAL VIDEO WIRED:** `src/runtime/mediaUrl.ts` canonical helper Tauri v2 `window.__TAURI__.core.convertFileSrc` or dynamic `import('@tauri-apps/api/core')` v2, browser dev degrades safely `null` to idle/fallback, `toAssetUrl`, `toAssetUrlSync`, `resolveMediaVerificationToUrls`, `pickGameplayFromResolved`. `App.tsx` uses it – when highlighted game changes real `GameEntry` → verify real scraped media → resolve → pass to stage not fabricate.

**SCREENSHOT FALLBACK:** Missing video → `primaryType none fallbackReason idle glass` truthfully no real scraped media → idle-glass present, no fake gameplay painted. Tests verify `pickGameplay({}) → {none, primaryUrl null}`.

**REAL PHYSICALMEDIA WIRED:** `selectedPhysicalUrl` resolved via `verification.physicalmedia? toAssetUrl` else `undefined` → passed into `SystemStage` `physicalMediaUrl`. Missing physical → renders nothing, not fake cartridge disc screenshots. Physical pass shares calibrated hardware-frame coordinate system static/rest position enough in V7.3 DO NOT start final cartridge/disc insertion animations yet slot targets remain preliminary.

**LOCAL TAURI MEDIA URL HANDLING:** Verify files show `mediaUrl.ts` uses canonical Tauri v2 file/asset URL mechanism, no `D:/Emulation/roms` hardcode only comment explaining slash forms, no `%USERPROFILE%` hardcode, browser dev safe null path, never invent media locations.

**NDS/3DS TRUTHFUL DUAL-SCREEN:** `src/stage/config/*.ts` `nds`, `n3ds` two independent regions distinct top 47.5%/23% bottom 43.9%/23.6% etc. V7.3 `src/App.tsx` explicit policy: NDS/N3DS preserve two independent regions DO NOT blindly copy same video twice – display truthfully using deliberate policy single combined preview shown in top primary only secondary idle. `pickGameplayFromResolved` docs dual-screen single source → primary only, documented limitation do not fake second source. WiiU hybrid similar. Verified PS2/GC/GBA single-region tested 1920+1280; dual-region theoretical only – still architecture supports.

**LIGHT MODE:** Cool white pale grey clear acrylic subtle blue/silver NOT beige NOT cream NOT hospitality shelf rails elegant cool-wash `filter blur(26px) brightness(0.84) saturate(0.88) scale(1.06)` storefront `saturate(1.05) brightness(0.92)`. Dark independent dark `blur(32px) brightness(0.68) saturate(0.82) scale(1.08)` graphite black crystal electric cool highlights deep glass. Idle glass light variant light cool.

**DARK MODE:** Graphite black crystal electric cool highlights deep glass, `blur(32px)…` background vignette mild local dimming behind left shelf optional `bg-hw-radial` subtle radial emphasis around hardware storefront `radial-gradient(ellipse 42% 54% at 66% 52%, rgba(125,249,255,0.08)…`.

**MOTION:** New background crossfades cleanly hero hardware transitions rather than popping selected shelf logo moves into selected position previous hardware may fade/slide ~40-80px new hardware enters corresponding direction modest scale 0.96→1 no excessive bounce avoid loading flash `cubic-bezier(0.16,1,0.3,1)` durations tasteful. `durFilter 420ms durTrans 560ms durHw 480ms durFade 380ms` spec 350-550ms 300-500ms. Reverse cleanly. Neighbours animated opacity/scale/blur shift. Respect `prefers-reduced-motion` 120-160ms.

**PRELOADING:** App preloads adjacent backgrounds/hardware/logos for previous/next ±2 for motion smoothness modalscale 0.96→1 40-80px slide. Memoized preload via `Map` Set.

**TESTS:** `bun test` → **102 pass 0 fail 350 expects across 11 files** – V6/V7/V7.1/V7.2 77 pass preserved + new V7.3 `tests/v73.showroom.test.ts` 25 pass verifying showroom placement category defaults distinct, ps2 large hero, gb/gbc tighter than tv, gba larger centred-right, steam wide, wiiu lower, shelf wrap logic prev prev/next next 5 visible, missing logo fallback full name never raw ID, media priority video > screenshot > title/mix/cover > idle glass none, missing physical renders nothing, structural outer wrapper exists inner contain math unchanged, storefront no hidden hardware, no carousel, library background blur isolated `.stage-bg-image` filter never `.hardware-frame/.layer-gameplay/.layer-physical`, utility rail moved.

**TYPECHECK:** `bun run typecheck` → `tsc -p tsconfig.app.json --noEmit && tsconfig.node --noEmit` exit 0 after fixing unused `useRef`, `logoUrl`, `isExampleData`, `SystemShelf React`, `isWindowsAbsolute/isUnixAbsolute`, `theme` unused in shelf/library via `_theme`. Files compile strict.

**VITE BUILD:** `vite build` → transforming 86 modules, `dist/index.html 0.59kB`, `dist/assets/index-BBVupELZ.css 11.42kB gzip 3.24kB`, `manifest-BylxuhXm.js 19.68kB gzip 4.26`, `index-BAAueQMZ.js 250.43kB gzip 73.14kB` built 2.31s – up from V7.2 235kB 69kB due to SystemLogo/SystemShelf/mediaUrl.

**CARGO CHECK:** Tauri `cargo` not available in web VM – attempted `which cargo` → no cargo, expected Windows host limitation, note in report, does not block presentation contract (Tauri runtime unchanged from V6 `main.rs` 19 systems real config).

**TAURI BUILD:** Deferred to Windows host same as V6 – V7.3 preserves V6 Tauri `main.rs` real `get_machine_config` no fake, `src-tauri/Cargo.toml` version bumped 3.5.0→3.6.0 consistent, `tauri.conf.json` 3.6.0.

**SCREENSHOTS PRODUCED:** Attempted 5 PNGs 1920×1080: PS2 storefront dark, GC storefront dark, GBA storefront light, PS2 library dark, GC library light via subagent `b46bda78` – same blocker as V7.2 `63f9ee27` browser VM cannot reach host `127.0.0.1:1420`/`198.19.0.2:1420` (`ERR_CONNECTION_REFUSED` isolated veth) – `playwright` install pending 52s+ no files after 90s. Previous V7.1 screenshots exist `/tmp/crystal-v71-ps2.png` 2.7M etc. Code-verification passed structural asserts: `.crystal-system-shelf` exists, `.hardware-showroom-wrapper` outer transform exists, `.hardware-frame` opacity 1 storefront, `.stage-bg-image` filter blur isolated, no `.carousel`. Report `/tmp/v73-screenshots-report.md` pending – block policy same as V7.2 `/tmp/vite-v72.log` dev 17978 LISTEN 0.0.0.0:1420 ready 280ms. Known limitation acceptable per V7.2 delivery.

**PRIVACY REGRESSION:** `.gitignore` covers `crystal-machine-config.json, machine-config.json, *.machine.local.json, CRYSTAL-MACHINE-AUDIT.md, *.zip` line 50-51. `git ls-files` no committed `crystal-machine-config.json`. Grep `src/` no `C:\Users\ciara` literal, README redacted `C:\Users\ciara → %USERPROFILE%`. Only generic `D:/Emulation` example appears in comment `src/machine/validation.ts` explaining slash tolerance – not private. No real ROM filenames, no scraped media paths hard-coded.

**KNOWN LIMITATIONS:** 1) Screenshot blocked by browser VM network policy – code-verified instead. 2) Dual-screen NDS/n3ds & WiiU hybrid regions architecture exists but not visually exercised via real Tauri game media (no ROM enumeration in web VM) – single-region PS2/GC/GBA verified 1920/1280 no drift. 3) Physical media insertion still static/rest – slot/tray target axis/path preliminary not measured (V7.3 scope deliberately defers animation). 4) Cargo/Tauri Windows host required for true Tauri build – not in web VM. 5) ZIP `Crystal-Hardware-Foregrounds.zip` not committed by policy – curated PNGs only under `public/assets/hardware/` 22 files 2.2-2.9 MB each 1024-1536px RGBA steam transparent preserved. 6) No trademarked logo reproduction 1:1 – SystemLogo uses theme asset swappable Canvas later.

**PUSH VERIFIED:** Local HEAD `28c895f` → remote `origin/main` `99ffda4..28c895f main->main` success 2072 ms, `git ls-remote origin main` `28c895f1a2ee3c3edcf35503c272c546ed155015 refs/heads/main` matches.

**READY FOR FIRST ROG BOOT:** V7.3 criteria met: storefront shows hardware hero (not hidden), storefront does NOT hide hardware, old full-width bottom navy carousel removed, calibrated inner hardware-frame unchanged by showroom outer transform, system shelf navigation wraps correctly, missing platform logo falls back to full system name not raw ID, selectedGame state updates to shared gameplaySources/physicalMedia, missing video falls back truthfully to idle glass, missing physical renders nothing, library background blur never affects hardware/gameplay layer (isolated `.stage-bg-image`), tests 102 pass, typecheck 0, vite 86 mods 250kB, privacy regression clean, version 3.6.0 V7.3 V7 family NOT V8, ready for ROG Ally X first-device testing.

---
**Blueprint for next:**
- When on Windows ROG: `bunx tauri dev --port 1420` verify real `get_machine_config` 19 systems populated, exercise NDS/n3ds two screens with real scraped video (single combined preview truthful policy), verify steam transparent monitor 2.16M no opaque, run `cargo check`.
- Screenshots on device: capture 1920×1080 PS2/GC storefront dark/light + library blurred/sharp hero.
- Kill dev servers after: `pkill -f vite` / PIDs 17978/19730 from `/tmp/vite-v72.log`.
- Delete buffer logs `/tmp/vite-v*.log`.

Ping: This concludes V7.3 – main agent will now ping chat with status update per 5-min rule.
