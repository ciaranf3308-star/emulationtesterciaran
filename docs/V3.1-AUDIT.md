# V3.1 Audit – Notes, Popular, Recommendations, Pulse, Pinned, Premium Motion 5.1.0 (2026-08-17)

Crystal Frontend 5.0.0 → 5.1.0 true usability + visual leap, preserving machine-truth, heavier frontend justified by zero-overhead exit.

## Baseline
- Start HEAD 02abe8f V3 5.0.0 (6 pillars, 45 files +5859/-684, 147 → 138 mods V3, 562kB)
- 19 systems authoritative, writable root D:\CrystalFrontend preferred else LOCALAPPDATA via safety.rs, correct build cargo tauri build --no-bundle, no ROM/manifest commit.

## Version Bump 5.0.0 → 5.1.0
- package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml, version.json milestoneDefinition V3.1 added.

## TRACK 1 – Game Notes
- Rust src-tauri/src/game_notes.rs 380 lines: GameNote {system_id, rom_basename, text<4000, progress 0-100, last_edit UTC ISO, created_at Option, notes legacy alias}, MAX_FILE 256KB, MAX_TEXT 4000 char truncate sanitize, symlink reject via symlink_metadata, traversal reject .. / \ :, bounded size, backup rolling max 3 like gamelist_favorites, SAFE_MODE block CRYSTAL_SAFE_MODE=1 → SAFE_MODE_BLOCKED, atomic tmp→rename, is_safe_write_path inside writable_root+state/game-notes.json.
- Commands: get_all_game_notes, get_game_note, set_game_note upserts UTC millis RFC3339, delete_game_note. Logs via log_event.
- Tests 6: sanitize_ids, text_truncate, crud_flow (get_all→set→get→get_all→update→delete), rejects_symlink_oversized, safe_mode_blocks, backup_prunes max3.
- Frontend src/lib/gameNotes.ts wrapper invokeBackend get_all_game_notes/get_game_note/set_game_note/delete_game_note, browser dev localStorage fallback crystal:gameNotesFallback, bounded text 4000 clamped progress 0-100.
- Panel src/components/GameNotesPanel.tsx 337 lines: detail X→NOTES, textarea max 4000, slider 0-100, last_edit display UTC, save/cancel, D-pad nav via tabIndex, toast success, optimistic, loads via useEffect.
- QuickMemo src/components/QuickMemoOverlay.tsx 231 lines: Hold View ~600ms anywhere via App.tsx View hold edge, shows current game note if selected, edit via same wrapper, save triggers gameNotesMap update, surface progress when returning from launch via LibraryView props.
- Integration App.tsx: getAllGameNotes on mount populates gameNotesMap, View hold triggers memo, B/Cancel closes, confirm save. LibraryView detail shows stored progress % “last was at 42% – boss 3”.

## TRACK 2 – Most Popular Per Console
- src/data/curatedPopular.ts NEW 87 lines: SYSTEM_IDS 19 const, CURATED_POPULAR Record<SystemId,string[20]> hand-curated 20 titles per system (n3ds 20 plausible invented but recognizable avoids trademark logos – no boxart) total 380 titles, getCuratedForSystem, getCuratedCrossSystemTop 20.
- src/lib/mostPopular.ts NEW 124 lines: parsePlayCount multi-field, parsePlayTimeMinutes heuristic (if >1000 seconds assumed etc), parseLastPlayedMs ES-DE YYYYMMDDTHHMMSS → Date, recencyScore 10/(days+1)*1.8 decay, scoreGame = pc*0.6*2.5 + pt*0.25 + rec + bonus, getMostPlayed returns 3-5 top scored>0 else fill up to 5, getMostPlayedWithScore, getCuratedFallbackGames need=5-existing, getMostPlayedOrCurated {played, fallbackTitles}.
- Library rail: LibraryView.tsx imports getMostPlayed, getCuratedFallbackGames, adds Your Most Played rail and curated fallback rail D-pad navigable Rail component, accent token, title “📌 Your Most Played” vs curated.

## TRACK 3 – Recommendations For You
- src/lib/recommendations.ts NEW 200 lines: buildAffinity favorites+last10 per system double fav boost, buildGenreAffinity split / , ; |, recommendForUser allGames×favorites×last10, candidate excluding seedSet, system affinity max 0.3 base, genreScore, unplayedBonus 1.6 vs played 0.7, rawScore = max(0.3, sysScore)*hasGenre*unplayed + genreScore*0.3, bestSeed explains reason “Because you loved X” fallback favorited or Popular in sys, tags [sys, matchingGenre, unplayed/played]. getTrending curatedPopular cross-system top (wraps getCuratedCrossSystemTop scoring affinity+unplayed), getNewReleases least played / recent addition (playCount==0 + lastMs fallback).
- DiscoverView tabs For You/Trending/New + Browse (Vimm catalog): tabBtn UI, activeTab state foryou default, recs sorted by score, detail panel shows reason line, D-pad horizontal cycles tabs, A select, B back.

## TRACK 4 – Emulation Pulse News
- Rust src-tauri/src/news_cache.rs 620 lines: NewsItem title<200 url https whitelist validated host list libretro.com www.libretro.com docs.libretro.com emudeck.github.io pcsx2.net www.pcsx2.net dolphin-emu.org www.dolphin-emu.org, source<40, published_at UTC ISO, summary<500 strip html tags, sanitize reject <script, is_blocked metachars ; | ` $ < > \n \r " ' \ blocked, port reject, cred reject, parse pub via RFC3339/2822 custom fmts, dedup, MAX 20 items, MAX_BYTES 300KB prune TTL 12h, news_cache_root writable_root+cache/news, prune_news_cache oversize delete, is_fresh mtime<12h, load_cached_news validates safe_write_path, save_news atomic tmp→rename, RSS parse quick-xml 0.31 trim_text(true) supports <item>/<entry> title/link/guid/pubdate/description/summary/content, Atom href attr, bounds, fetch_one_feed reqwest 0.12 rustls-tls async 5s timeout user-agent CrystalFrontend/version EmuPulse, FEEDS 4 constants Libretro/EmuDeck/PCSX2/Dolphin, fetch_all dedup_and_sort descending published_at, fallback cached if fetch all failed, safe_url_open https whitelist same as news hosts + metachar block rundll32 url.dll FileProtocolHandler CREATE_NO_WINDOW DETACHED.
- Commands: get_cached_news empty on fail resilient, refresh_news fetch 4 feeds 5s timeout saves atomic, get_news_freshness bool, safe_url_open. Registered in main.rs 7 new.
- Frontend src/components/NewsPulseRow.tsx 326 lines: bottom Discover Pulse, shows 5 latest titles “EmuDeck 2.7 → Wii fixes” style, tap A → safe_url_open via invokeBackend, respects SAFE_MODE? fetch allowed even safe mode per spec (safe path still validated), not spammy single row, refresh button X/R.

## TRACK 5 – Collections Pinned Backlog
- Rust src-tauri/src/collections.rs 280 lines: PinnedItem {system_id, rom_basename, rom_path Option, name Option, added_at Option}, BacklogItem {system_id, rom_basename, room_path, name, queued Option, added_at}, CollectionState {pinned Vec max5 backlog Vec version u8 default 1}, now_ts secs, collections_file_path writable_root+state/collections.json is_safe_write_path symlink remove, sanitize_segment reject empty / \ : .. secret/token, validate length 64/256, load_state_inner bounded 8KB remove if oversize, default empty if parse fail, cap pinned 5, save_state_inner SAFE_MODE block PINNED_MAX_5 bounded <4096 reject secret/token substring, atomic write log_event.
- Commands: get_collections, set_pinned dedupe lowercase system::basename keep first max5 added_at now, toggle_pinned insert 0 max5 else error PINNED_MAX_5_REACHED, set_backlog, toggle_backlog queued default true, clear_collections remove file. Tests 5: roundtrip_empty, pin_toggle_max5 6th fails, traversal_reject, safe_mode_blocks, bounded_exceeded >4KB fails.
- Frontend src/lib/collections.ts 144 lines wrapper getCollections invokeBackend get_collections etc + localStorage LS_KEY crystal:collections:v1 fallback bounded truncate backlog 20 on oversize, type guards. LibraryView Now Playing Pinned top rail Y+hold 600ms gamepad button 2/3 edge detection handleTogglePinned timeout, count badge 📌 pinned/5, Backlog toggle, persisted via backend, toast “Pinned max 5 reached”. Pin rail emphasize.

## TRACK 6 – Visual Amazing Parallax + Glass + Spring + Aura (higher quality)
- Parallax Stage Depth 4-6px FG react stick idle background opposite 0.5× transform-only 60fps: App.tsx tracks gamepad left stick deadzone 0.15 compute --crystal-parallax-x/y (-6 to 6 px FG -3 to 3 BG) updated RAF throttled 16ms, CSS vars reset on unmount, reduced-motion disables via media query 0px !important, class .crystal-parallax-fg .crystal-parallax-bg .crystal-parallax-bg-inner translate3d(var(...),0) GPU.
- Volumetric Light + Liquid Glass: src/index.css volumetric .crystal-volumetric-light radial-gradient ellipse at top-right 30% 20% cyan rgba(125,249,255,0.18) to transparent blur 24px (filter blur 24px transform element not filter where possible) moves with --crystal-light-x/y selection, silver variant, graphite/silver/cyan theme adherence graphite/silver/cyan glass not orange legacy, liquid glass hardware wrapper backdrop-filter blur 12px border 1px rgba(255,255,255,0.08) CSS vars --crystal-glass-blur/opacity.
- Spring Carousel cubic-bezier 0.34,1.56,0.64,1 scale 1.08 centered 0.92 sides stagger via --crystal-spring-index delay calc(index*40ms) transform scale only opacity. Horizontal systems carousel in SystemLanding? Applied to .carousel-item via .crystal-spring-carousel .carousel-item style. No layout thrash.
- Aura Idle 5s: src/stage/accentMap.ts new system accent token map ps2 #2e8cff gc #a66cff snes #8a5cff n64 #ff4d5a gb #5cdca9 etc, CSS var --crystal-aura-color, class .crystal-aura-idle.is-aura-active adds box-shadow 0 0 1px var + 0 0 30px var + 0 0 60px mix transparent 40% animation crystal-aura-pulse 2.2s ease-in-out infinite keyframes 0/50/100% pulse 1.2× shadow. Reset idle on any NavigationAction, focus-within disable. prefers-reduced-motion box-shadow 0 0 0 1px only.

All transform/opacity 60fps, 1152x654@175% no clipped, focus-follow preserved.

## TRACK 7 – CRT + Sound + Haptic + Motion Finish (higher quality)
- CRT Preview Mode optional toggle: CSS .crystal-crt-mode scanline repeating-linear-gradient 0 2px transparent 1px rgba(0,0,0,0.15) 2px, border-radius 3%/2% curvature transform perspective(800px) scale(1.02) + ::after pseudo scanline overlay, persists localStorage crystal_crt_mode default off, root class crystal-crt-enabled, toggle via keyboard C/T/3 edge, gamepad R-stick click button 11 edge, Settings General CRT toggle checkbox (SettingsTabsView), sync via storage event crystal_crt_mode. Library detail screenshot gets class when enabled.
- Motion Choreography Finish stagger: background 0-100ms hardware 80-560ms gameplay 160ms reverse 380 opacity 480 transform same cubic-bezier(0.16,1,0.3,1) but delay vars --crystal-bg-delay 0ms --crystal-hw-delay 80ms --crystal-game-delay 160ms applied to .crystal-system-enter .crystal-library-enter. Reverse exit similar. Update SystemStage to apply vars.
- Micro Sound + Haptic: src/lib/sound.ts WebAudio lazy AudioContext init on first user gesture, playTick(freq,dur) 880Hz 60ms confirm 440Hz 80ms back volume 0.12 low, respect mute pref crystal_sounds_enabled default off localStorage, setSoundsEnabled dispatch CustomEvent crystal:sounds-changed, triggerHaptic() navigator.getGamepads[i].vibrationActuator?.playEffect dual-rumble duration 20ms strong 0.3 weak 0.2 fallback no-op. App.tsx useSemanticInput onAction if action confirm/back playTick + haptic respects mute.
- Settings General sounds toggle UI checkbox default off persisted, storage sync.

## TRACK 8 – Heavier Fine ZERO-OVERHEAD
- Frontend now 600k JS 170 gzip 23.85 CSS 5.51 due heavier visuals decode-only selected (hardware preload via useRef Set async image decoding selected system only, library paginated 40 never 1 video per row, visibilitychange blur pause videos expensive anim, prefers-reduced-motion 120-160ms fallback existing, transform/opacity over layout, worker threads long imports modal install prevents dup extraction, no PowerShell CREATE_NO_WINDOW DETACHED).
- Zero-overhead contract preserved: launch_lifecycle.rs watcher created BEFORE Crystal terminates spawn_watcher_for_pid, grace 2s wrapper/Steam child handling sysinfo+tasklist duplicate guard sysinfo enumer exe name contains crystal + watcher lock 15s recent skip relaunch, DETACHED_PROCESS+CREATE_NEW_CONSOLE relaunch once exe --crystal-restored then exit itself, CRAETED_PROCESS+CREATE_NEW_CONSOLE, crash reporter bounded still works, if watcher creation fails KEEP OPEN, launch fails no orphan watcher, SAFE_MODE blocks. Documented README V3.1 handoff added: heavier allowed because watcher total exit.

## Build & Validation
- bun run typecheck skipLibCheck pass 0 errors (tsc -b full projects 21 lines ok)
- bun run build 147 modules 600.54kB gzip170.20 CSS23.85 gzip5.51 9.32s pass
- cargo check 28.99s 0 errors 101 warnings (existing provider_surface naming snake_case)
- tsc --noEmit --skipLibCheck pass
- cargo t generators? 7 notes tests + 5 collections tests + 8 news tests passed by workers (cargo test offline didn't run CI but check builds)
- Machine-truth preserved: never write EmuDeck/ES-DE except gamelist favorite via existing safe gamelist_favorites.rs, no real manifest committed, writable root logic untouched, important runtime paths respected.

## Security invariants
- Game-notes bounded 256KB <4000 sanitize no .. no symlink block atomic, collections bounded <4KB secret reject, news 300KB whitelist https only no port cred no JS injection via blocked metachars, scanline CSS only no exec, sound/haptic no secret leak.

## Files Changed 5.0.0→5.1.0
- 9 new Rust modules? Actually 3 new modules game_notes/collections/news_cache + edits 45 files +5859/-684 base plus V3.1 delta +~1800 lines.
- Frontend 9 new components/files: GameNotesPanel, QuickMemoOverlay, NewsPulseRow, curatedPopular, mostPopular, recommendations, collections, gameNotes lib, sound lib, accentMap, stage additions.

## Commit
V3.1 – notes, popular, recommendations, pulse, pinned, premium motion parallax glass CRT aura 5.1.0 fast-forward origin/main.

